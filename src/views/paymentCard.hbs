{{!-- <div class="container-fluid my-10">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8">
            <div class="row">
                <div class="col-md-2">
                    <div class="card border-none">
                        <img src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_1280.png"
                            class="w-16 h-16 object-cover rounded-circle img-thumbnail" alt="Imagen de perfil" />
                    </div>
                </div>
                <div class="col-md-10 mt-4 mb-4">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            {{#if user}}
                            <h1 class="m-2">Bienvenido {{user.name}}</h1>
                            <p class="m-2" id="userEmail">{{user.email}}</p>
                            <h2 class="m-2">Permisos: {{user.role}}</h2>
                            {{else}}
                            <h1>Bienvenido Invitado</h1>
                            {{/if}}
                        </div>
                        <div>
                            <form class="m-2" action="/products" method="get">
                                <input type="submit" value="Productos" class="btn btn-dark" />
                            </form>
                            <form class="m-2" action="/chat" method="get">
                                <input type="submit" value="Chat" class="btn btn-dark m-2" />
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card mt-4">
                <div class="card-header text-center">
                    <h3>Payment Details</h3>
                </div>
                <div class="card-body text-center">
                    <form id="paymentForm" action="/api/payments/:ticketId/purchase/" method="post">
                        <div class="form-group text-center">
                            <label for="cardNumber">Card Number</label>
                            <input type="text" class="form-control text-center fw-lighter" id="cardNumber"
                                name="cardNumber" required pattern="[0-9]{13,16}"
                                title="Please enter a valid credit card number" />
                        </div>
                        <div class="form-group text-center">
                            <label for="expiryDate">Expiration Date</label>
                            <input type="text" class="form-control text-center fw-lighter" id="expiryDate"
                                name="expiryDate" placeholder="MM/YY" required pattern="(0[1-9]|1[0-2])\/[0-9]{2}"
                                title="Please enter a valid expiration date (MM/YY)" />
                        </div>
                        <div class="form-group text-center">
                            <label for="cvc">CVC</label>
                            <input type="text" class="form-control text-center fw-lighter" id="cvc" name="cvc" required
                                pattern="[0-9]{3}" title="Please enter a valid CVC number" />
                        </div>
                        <div class="form-group text-center">
                            <label for="country">Country</label>
                            <input type="text" class="form-control text-center fw-lighter" id="country" name="country"
                                required />
                        </div>
                        <input type="hidden" id="ticketId" name="ticketId" value="{{ticketId}}" />
                        <button type="submit" class="btn btn-dark mt-4">Pay Now</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> --}}